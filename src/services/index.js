import {
  count,
  findOne,
  findAll,
  createOne,
  updateOne,
  deleteOne,
} from "./dbQuery.services.js";

/**
 * Services index module.
 *
 * @module Services
 * @author Saurav Pandey
 * @version 1.0.0
 */

// Exporting all query methods for use in other parts of the application.
export { count, findOne, findAll, createOne, updateOne, deleteOne };
